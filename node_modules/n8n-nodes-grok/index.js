const { Grok } = require('./dist/nodes/Grok/Grok.node');
const { GrokApi } = require('./dist/credentials/GrokApi.credentials');

module.exports = {
    nodes: [
        Grok
    ],
    credentials: [
        GrokApi
    ],
    version: require('./package.json').version,
};
